<div class="w-full bg-white p-3 sticky top-[134px] border-b">
    <a href="{{ $path }}" class="text-sm pb-0 text-gray-400">{{ $pageRoot }}</a>
    <h1 class="text-lg font-semibold">{{ $pageName }}</h1>
</div>